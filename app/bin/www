#!/usr/bin/env node
// var debug = require('debug')('canvas-server');
var app = require('../app');

app.set('port', process.env.PORT || 3000);

var server = app.listen(app.get('port'), function() {
  console.log('Express server listening on port ' + server.address().port);
});


//Sockets
// const eventBus = require('../pubsub');

// const io = require('socket.io')(server);
// io.on('connection', function(socket) {
//   console.log('client connected');
//
//   socket.join("Publish and Subscribe");
//
//   socket.on('disconnect', function() {
//     console.log('client disconnected');
//   });
// });
//
// eventBus.on('favorite.deleted', function(event) {
//   io.to("Publish and Subscribe").emit('favorite.deleted', event);
// });
// eventBus.on('favorite.created', function(event) {
//   io.to("Publish and Subscribe").emit('favorite.created', event)
// });
//
// eventBus.on('favorite.updated', function(event) {
//   io.to("Publish and Subscribe").emit('favorite.updated', event)
// });
